name: dievas
publish_to: none
description: "Dievas design system — Flutter theme and component layer."
version: 0.1.0
homepage:
resolution: workspace

environment:
    sdk: ">=3.11.4 <4.0.0"
    flutter: ">=3.22.0"

dependencies:
    flutter:
        sdk: flutter
    equatable: ^2.0.5
    dievas_tokens:
        path: ../dievas_tokens

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^6.0.0

flutter:
