name: dievas_tokens
description: A starting point for Dart libraries or applications.
version: 0.0.1

resolution: workspace

environment:
    sdk: ^3.11.4

dev_dependencies:
    lints: ^6.0.0
    test: ^1.30.0
