/// The Example App brand theme.
///
/// This folder is the template every consumer app copies to define its own
/// brand: raw tokens → typography → light/dark theme data. Study the four
/// files in order:
///
/// 1. `example_tokens.dart` — raw brand values (colours, fonts)
/// 2. `example_typography.dart` — the type ramp built over Dievas tokens
/// 3. `example_light_theme.dart` / `example_dark_theme.dart` — the theme classes
library;

import 'package:dievas/dievas.dart';
import 'package:dievas_tokens/dievas_tokens.dart';
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

part 'example_tokens.dart';
part 'example_typography.dart';
part 'example_light_theme.dart';
part 'example_dark_theme.dart';
